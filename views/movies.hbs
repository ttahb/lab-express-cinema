<div id="movie-list">
    {{#each movies}}
    <div class="movie">
        <img src="{{image}}" alt="movie-img">
        <h2>{{title}}</h2>
        <a href="/movies/{{_id}}">See more</a>
    </div>
    {{/each}}
</div>